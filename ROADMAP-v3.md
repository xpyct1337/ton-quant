# TON Quant v3.0 — AI Smart-Money Desk (роадмап)

Версия v3.0: поверх v2 (сбор данных в GitHub Actions + сайт SvelteKit) встаёт слой
локальных LLM-агентов на M1 Mac — манипуляция-устойчивый, обоснованный copy-фид.
Спек: `docs/V3.0-AI-SMART-MONEY-DESK.md`. Альфа (evolve-run #1–#45) заархивирована в
`ROADMAP-alpha-archive.md`.

Архитектура (три слоя, связь только через файлы в репо):
`[Сбор] GitHub Actions → data/*.json` · `[Десk] M1 launchd → data/desk/verdicts.json` ·
`[Витрина] SvelteKit статика → читает verdicts.json`.

---

## 🚀 11.07.2026 — production mindmap и wallet drill-down

Опубликован новый control surface `/mindmap/`: исследовательский цикл представлен
интерактивными узлами «Источники идей → Гипотеза → Минимальный эксперимент →
Forward/paper-test», а также ветвями Desk, витрины, журнала решений и data lineage.
Кнопка `Submit` отправляет выбранную ветку в Codex task, если доступен app-контекст,
иначе открывает prefilled GitHub Issue — без токенов и без прямой записи в репозиторий.
Mindmap дополнительно показывает текущий evidence-status из `calibration.json` и
ведёт в `/desk/`, чтобы выбор следующего шага и проверка результата были в одном цикле.
При пустом IS mindmap предлагает маршрут `data → собрать IS-историю` одним кликом.

Добавлены `/wallet/<address>/` для двух адресов Pavel Durov и текущего smart-money
roster. Страница статически маршрутизируется, но holdings и баланс обновляются из
TONAPI client-side; это минимальная основа для следующих wallet-specific блоков,
не выдающая текущий balance за историческую доходность.

Production evidence: commit `cbc32a7`, GitHub Actions `Deploy site` и `Verify`
завершились успешно для `headSha=cbc32a7`.

## 🚦 11.07.2026 — calibration gate: pairwise separation недостаточна

Калибровка на 29 snapshots показала `+7d excess`: low `-0.097`, med `+0.531`,
high `-0.219`. Старый флаг проверял только `high < low` и поэтому ошибочно
помечал U-образный результат как подтверждение сигнала. Добавлен строгий
point-in-time gate `high < med < low`; текущий результат — `false`, фактор не
промоутится. Pairwise-метрика сохранена отдельно для диагностики.

Следующий кандидат без новых API-вызовов — forensics `bundle > 20%`: +7d excess
`-0.095` (n=19) против `+0.029` (n=62). Это только descriptive candidate:
walk-forward gate дал IS `n=0`, OOS `n=19`, Wilson `62.4`, `p=0.0044`,
`passed=false`. OOS-эффект интересен, но без исторического IS-прогона
автоматического промоута нет.

## 🚀 Готово 01.07.2026 — Track A+B: расширение сигналов и сбора данных (по ресёрчу Igor)

**Track A (без изменений сбора):** временные ряды из дневной истории снапшотов в DSL
ресёрчера — `ret_1d` (reversal), `mom_3d/7d` (momentum), `vol_z` (аномалия объёма),
`rvol`, `trend`, `hgrow_7d`. Разблокировало сильнейшее академическое семейство
(cross-sectional momentum/reversal), к которому ресёрчер был слеп. Плюс ротация
фокус-семейств с жёстким меню полей на раунд — 4B залипал на одной идее, игнорируя
avoid-лист.

**Track B (новые сборы; ключевая находка — оба облачных сборщика уже фетчили нужное
сырьё и выбрасывали его, поэтому 2 из 3 наборов стоят 0 новых API-вызовов):**
- `snapshot.py` (+0 вызовов): `spread` (межпуловый разброс цены), `top_pool`
  (хрупкость ликвидности), `age_d` (возраст токена), `admin` (сид deployer-реестра).
- `wallets.py` (+0 вызовов): `data/holders/<date>.json` — `top10`/`top25`/`hhi` из уже
  фетчащихся балансов. Живьём: TAC top10=90.6% supply, один токен 99.75%.
- `flows.py` (новый, GeckoTerminal, non-fatal): `ubuyers`/`buyer_conc`/`buy_share`.
  Живьём дискриминирует: TAC 300 сделок/20 покупателей/conc 99.7% (боты) vs TONY
  107 покупателей/52% (органика).
- `social.py` (новый, t.me/s/ публичные превью, без auth, non-fatal): кэштег-упоминания
  ($SYM/#SYM — точность важнее полноты, голые слова типа NOT/COIN не считаются) по
  каналам из `data/social_channels.json` (Igor курирует список).
- `intraday.yml` (новый workflow, 15:10 UTC): второй дневной срез → `data/intraday/` —
  удвоение разрешения временных рядов, дневная семантика снапшотов не тронута.

**Деск:** `load_aux()` вливает holders/flows/social в фичи и исторический гейт;
`AUX_FIELDS` в DSL; 4 новых фокус-семейства ресёрчера. Первые данные засеяны с Мака
(та же кодовая база, что гоняет Actions).

**Следующее по сбору:** deployer-history скоринг (admin уже копится → реестр
повторных раггеров), transfer-таймлайн форензика (bundling/снайперы — тяжёлый фетч,
отдельный спек), pump-классификация сообщений локальным LLM (PumpSense-подход, когда
в конфиге появятся трейдинг-каналы).

---

## 🚀 Готово 30.06.2026 — закрытие бэклога Phase 1/2/3, витрина на `/wallets`

**Витрина `/wallets` (спек §8, D1/D2 — последний незакрытый пункт исходного WBS).**
Риск-значок (low/med/high) на каждой карточке кошелька + CoT-обоснование деска в
тултипе; «грязные» (`copy_ok=false`) тускнеют и уходят вниз, вётченные деском —
наверх; кошельки без вердикта не трогаются (не наказываем за отсутствие данных).
На favorites — предупреждающий бейдж для токенов с `manip_risk: high`. Переиспользует
`loadDeskStatus()` (уже читал `verdicts.json`), гард на отсутствие деска сохранён.

**Баг: гейт агента-ресёрчера был навсегда закрыт.** `trials_count()` считал каждую
попытку, а не уникальную идею — за ~2 дня 24/7 набралось 12 238 попыток (ресёрчер
как idle-filler перепредлагает 2–3 идеи по кругу), анти-оверфит планка схлопнулась
до недостижимого p≈0.0007. **0 промоутов за 12k+ попыток — структурная поломка, не
невезение.** Фикс: считать различные `expr`, а не сырые попытки (34 уникальных на
всю историю) → планка вернулась к ~0.0125, гейт снова живой (проверено: гейт теперь
возвращает реальные числа, не вечный `None`). Заодно закэпили `factors_history.json`
(ring buffer, `MAX_HISTORY=1000`) — файл разросся до ~6 МБ и целиком перезаписывался
в git каждые 30 мин; разово обрезали 12238 → 1000 записей.

**Отставка ночного fallback.** `scripts/desk_run.sh` + `com.tonquant.desk.plist`
удалены — 24/7-воркер (`com.tonquant.worker`) единственный путь с 28.06, пережил
несколько циклов рестарта в этой сессии (устойчивость подтверждена делом). Попутно
нашли и закрыли реальный регресс: у `desk_run.sh` была логика поднять Osaurus, если
сервер не отвечает, а у `desk_worker.py` — не было; перенесли (`ensure_osaurus()`,
проверяется каждую итерацию, не только при старте) и проверили живьём (убил Osaurus
→ воркер сам поднял и продолжил цикл).

**Следующее:** `signal-factory` (спек `docs/superpowers/specs/2026-06-26-*`) —
отдельная фича (OKX-перпы, не десk), спек есть, план/код нет; масштаб на порядок
больше остального бэклога (10 архитектурных юнитов, staged A→B→C) — требует
отдельного решения по объёму прежде чем писать план.

---

## 🚀 Готово 28.06.2026 — запуск деска на Mac (MVP, Phase 1)

**AI Smart-Money Desk поднят и прогнан на живых данных на MacBook Air M1 (8 ГБ).**
Перенос v3.0 со спека в работающий рантайм. Сбор данных остаётся в облаке (Actions),
деск читает закоммиченные `data/` и пишет вердикты обратно в репо.

**Рантайм.** Бэкенд — **Osaurus** (нативный Apple-MLX, `:1337`), модель `qwen3-4b-4bit`:
на M1 быстрее/легче Ollama (~3 с/вызов с `/no_think` против ~7 с) и отдаёт чистый JSON
через OpenAI-совместимый `/v1/chat/completions` (`response_format: json_object`). Спек
ошибочно считал MLX недоступным на 8 ГБ — на деле 4B-MLX идёт отлично. Ollama (`qwen3:4b`/
`gemma3:4b`, `:11434`) остаётся запасным бэкендом; `desk.py` backend-agnostic (один
OpenAI-клиент, выбор через `data/desk/config.json`).

**Детерминированный слой фич (`scripts/desk_features.py`).** На roster-кошелёк/токен
из существующих `data/` (без новых фетчей): `wash` (переиспользует ban-лист
wash-детектора), `co_entry` (координация входа из диффов `data/wallets/<date>.json`),
`vol_auth` (аутентичность объёма: vol24/TVL + перекос buys/sells), `conc`
(концентрация холдеров — MVP-прокси по числу холдеров; реальный top10/HHI = Phase 2),
`edge_dispersion`. Все нормированы 0..1, self-check на живом срезе.

**Агенты (`scripts/desk.py`).** Клиент Ollama (urllib, таймаут+retry) + агент-1
«манипуляция» (`{manip_risk,flags,reason}`) + агент-2 «вёттинг»
(`{copy_ok,conviction,reason}`). Идут последовательно (одна модель в памяти),
отказоустойчиво (битый LLM-вызов → детерминированный floor, прогон не падает).
Детерминированные пороги — жёсткий гейт (wash-ban → high; high → `copy_ok=false`),
LLM только синтезирует обоснование и может поднять риск, но не опустить ниже floor.

**Проверка (`scripts/desk_test.py`, §10, на реальных данных).** Прогон деска на живом
`data/` → схема `verdicts.json` валидна; все wash-баненные токены (`data/paper/bots.json`
→ `wash_ban`) получают `manip_risk: high`; ни один high-risk кошелёк не `copy_ok`.

**Оркестрация.** `scripts/desk_run.sh` (pull → ensure Ollama → desk → commit/push) +
launchd `com.tonquant.desk` (ночь 03:30 + `RunAtLoad` catch-up). Гайд:
`docs/DESK-SETUP-m1.md`.

**Следующее:** D — витрина `/wallets` (`loadVerdicts()` + риск-значок/CoT-тултип/сортировка
грязных); полный ночной прогон всего ростера; Phase 2 — агент-ресёрчер альф под CPCV/PBO
и глубокая on-chain форензика (transfer-таймлайн → настоящий бандлер/снайпер-детект).
