# TON Quant — роадмап метрик и дашбордов

Что добавить, чтобы было информативнее и полезнее. По тирам: от быстрых побед к киллер-фичам.

## ✅ Готово 23.06.2026 (evolve-run #35, Opus, scheduled) — требует deploy.bat

**index.html: «Reversal watch» — новая карточка `#revCard`, объединяет RSI- и MACD-дивергенции в один сигнал двойного подтверждения.** Снято прямо из «Следующее» #34 («объединить RSI+MACD дивергенции в один reversal watch бейдж — двойное подтверждение сильнее»). Карточка добавлена в pane Momentum сразу после MACD-карточки.

**0 новых запросов** — целиком переиспользует уже валидированные `rsiDiv` (#32) и `macdDiv` (#34) на тех же запечённых `spark[]`. Новой математики нет, только синтез.

**Логика (`reversalWatch()`):** для каждого CORE-токена берутся обе дивергенции. Обе молчат — токен не на радаре. **Обе в одну сторону** → `confirmed` (двойное подтверждение). **Только одна** → `single` (раннее предупреждение). **Конфликт** (одна bull, другая bear) → отбрасывается как неактионируемый. Сортировка: confirmed выше single. Колонки: token / reversal signal (▲ bullish / ▼ bearish) / confirmed by (чипы RSI+MACD) / strength.

**Зачем:** одиночная дивергенция шумная. RSI (ограниченный осциллятор) и MACD-гистограмма (безграничный тренд-фолловер) ортогональны; их совпадение — куда более сильный сигнал разворота. Две отдельные колонки этого синтеза не давали — приходилось глазами сверять две карточки.

**Данные на сборку (real-data self-check, Node-порт `rsiDiv`+`macdDiv`+combine против запечённого `const TOKENS`, 20 CORE-токенов):** **STORM — bearish, confirmed** (RSI+MACD), **GTA — bullish, confirmed** (RSI+MACD), **HMSTR — bearish, single** (только MACD), **MAJOR — bullish, single** (только MACD), остальные none. Согласуется с раздельными прогонами #32 (RSI: STORM bear, GTA bull) и #34 (MACD: HMSTR/STORM bear, MAJOR/GTA bull) — пересечение даёт ровно STORM+GTA как confirmed. **PASS 6/0:** все actionable-типы ∈ {bull,bear}; conf ∈ {both,rsi,macd}; каждый `both`-ряд переподтверждён (rsiDiv===macdDiv===type); каждый `rsi`-single — только RSI; отброшенные ряды — реальные bull/bear-конфликты; scale-invariance (×1e6 → тот же вердикт). Конфликтов в снапшоте не оказалось (0 отброшено).

**Блокеров по пути не было.** Правки Edit-инструментом (Windows-side); целостность ТОЛЬКО Read-tool'ом: `reversalWatch()` (стр.1037–1074), карточка `#revCard` (стр.289–293), вызов в analytics-boot (стр.550), `</script></body></html>` (стр.1315–1317) на месте. bash-маунт QUANT усекает хвост index.html — проверка через Read-tool. git из песочницы НЕ запускался — деплой через `deploy.bat`.

Тесты: real-data self-check + 6 ассертов (PASS). API живые (TONAPI/STON.fi/DexScreener — 200).

Следующее: copy-trading (топ-кошельки TON через TONAPI) — главная money-фича; risk-return — фильтр «скрыть микрокапы <$X»; лид-лаг с лагом 2 дня; в «Today's signals» (paper.html) подмешать reversal-watch как фильтр доверия.

## ✅ Готово 23.06.2026 (evolve-run #34, Opus, scheduled) — требует deploy.bat

**index.html: MACD divergence — колонка «divergence» в карточке `#macdCard`.** Снято прямо из бэклога «дивергенцию пометить и в MACD-карточке (цена новый хай при падающей гистограмме)» — фигурировало в «Следующее» несколько циклов. Это близкий порт уже валидированного `rsiDiv` на гистограмму MACD.

**0 новых запросов** — переиспользует те же запечённые `spark[]`, что corr/lead-lag/risk-return/drawdown/momentum/breadth/RSI/MACD. Новая `macdArr(s)` поднята из тела `macdHist` (возвращает полный ряд base-100 цены + полную гистограмму, чтобы не дублировать EMA-логику; `macdHist` теперь вызывает её). `macdDiv(s,L=5)` сравнивает ценовой экстремум последнего окна L=5 с предыдущим окном L=5: **bearish** = более высокий ценовой хай при более низком хае гистограммы (моментум выдыхается у нового хая), **bullish** = более низкий ценовой лоу при более высоком лоу гистограммы (продажи иссякают). В таблицу добавлена колонка (▲ bull зелёным / ▼ bear красным, tooltip с определением), в note — список активных дивергенций, в card-tooltip — пояснение.

**Зачем, если уже есть состояние/кроссовер MACD:** уровень/знак гистограммы говорит «моментум растёт/падает сейчас», кроссовер — «только что развернулся»; дивергенция ловит ослабление тренда ДО кроссовера — цена ещё печатает новый хай, но гистограмма уже не подтверждает. Ортогональна и состоянию MACD, и RSI-дивергенции (RSI = ограниченный осциллятор, MACD = безграничный тренд-фолловер; могут расходиться).

**Данные на сборку (real-data self-check, Node-порт против запечённого `const TOKENS`, 22 CORE-токена):** **HMSTR — bearish** (dh −0.372), **STORM — bearish** (dh −0.709), **MAJOR — bullish** (dh +0.045), **GTA — bullish** (dh +0.707), остальные — none. PASS 28/0: тип ∈ {bull,bear,null} у всех; каждая реальная детекция прошла определительную инварианту (bear: priceHH & histLH; bull: priceLL & histHL); монотонный рост → не bull, монотонный спад → не bear; scale-invariance (×1e6 → тот же тип); серия <35 → null.

**Блокеров по пути не было.** Правки внесены Edit-инструментом (Windows-side); целостность подтверждена ТОЛЬКО Read-tool'ом: `macdArr`/`macdHist`/`macdDiv` (стр.974–995, по одному определению каждого), `div:macdDiv` в row-build (стр.998), `divCell`+колонка+note (стр.1005–1022), `macd()` в analytics-boot (стр.545), `</script></body></html>` (стр.1266–1268) на месте. bash-маунт QUANT усекает чтение/запись хвоста index.html (bash увидел 1186 строк без `</html>`; Read-tool — реальные 1268) — вся проверка целостности через Read-tool. git из песочницы НЕ запускался — деплой через `deploy.bat`.

Тесты: real-data self-check + 28 ассертов (PASS, см. выше). Тривиальная отрисовка (divCell по образцу RSI-divCell) юнита не требует (conytail). API живые (TONAPI/STON.fi/DexScreener — 200).

Следующее: copy-trading (топ-кошельки TON через TONAPI) — главная money-фича; risk-return — фильтр «скрыть микрокапы <$X»; лид-лаг с лагом 2 дня; объединить RSI+MACD дивергенции в один «reversal watch» бейдж (двойное подтверждение сильнее).

## ✅ Готово 22.06.2026 (evolve-run #33, Opus, scheduled) — требует deploy.bat

**paper.html: колонка «Edge» в карточке Open positions — conviction каждой открытой позиции.** Money-first, снято из «Следующее» (conviction-метка в open positions). «Today's signals» показывает edge для *новых* сигналов дня; здесь тот же вердикт прикладывается к позициям, что бот **уже держит** — мгновенно видно, какие из них стоят на доказанном крае (зелёная 1.00), какие на полу-probe (янтарная 0.25), а какие держатся на сигнале, который в scoreboard теряет деньги (красная 0.00 = noise/negative).

**0 новых эндпоинтов** — тот же `data/signals/scores.json`, что уже тянут scoreboard и Today's signals (локальный путь + GH raw fallback), добавлен в общий `Promise.all` бутстрапа.

**Ключевое:** `conviction()` из Today's-IIFE поднята до общей `convict(a)` (точный порт SIZE-ветки `load_score_mults` paper.py: SHRINK=3, FULL=4, FLOOR=0.25). Для каждой позиции `convict(per[p.signal])` → цветной pill со значением conviction (≥0.6 good / >0 warn / 0 bad) + tooltip с вердиктом и walk-forward. Так на экране ровно тот вес, которым бот реально сайзил бы этот сигнал сегодня.

**Данные на сборку (real-data self-check, Node-порт против настоящего scores.json, 6 типов сигналов):** hidden_buyer (walk-forward confirmed) → **1.00** (зелёный), flow_imbalance (collecting, x>0) → **0.25** floor (янтарный), accum_div/momentum (noise) → **0.00** (красный), отсутствующий тип → 0. PASS: conviction∈[0,1] у всех 6; ok-ветка (neutral+wf) =1; floor-ветка =0.25; noise/missing =0 — все ассерты прошли. Логика байт-в-байт совпадает с уже валидированной `conviction()` (та проверена против paper.py diff<1e-9).

**Блокеров по пути не было.** Правки внесены Edit-инструментом (Windows-side); целостность подтверждена ТОЛЬКО Read-tool'ом: `convict`/`loadScores` (стр.262–272), `Promise.all`+`per` (стр.273–275), Edge-колонка в шапке (стр.87, colspan 8→9 стр.88), conviction-ячейка в `posBody.map` (стр.336), `</script></body></html>` (стр.454–456) на месте. bash-маунт QUANT по-прежнему усекает чтение/запись хвоста — вся проверка через Read-tool. git из песочницы НЕ запускался — деплой через `deploy.bat`.

Тесты: real-data self-check + 5 ассертов (PASS, см. выше). Тривиальная отрисовка pill — без юнита (conytail). API живые (TONAPI/STON.fi/DexScreener — 200).

Следующее: copy-trading (топ-кошельки TON через TONAPI) — главная money-фича; MACD-дивергенция (цена новый хай при падающей гистограмме); risk-return — фильтр «скрыть микрокапы <$X»; лид-лаг с лагом 2 дня.

## ✅ Готово 22.06.2026 (evolve-run #32, Opus, scheduled) — требует deploy.bat

**index.html: RSI divergence — колонка «divergence» в карточке `#rsiCard`.** Снято прямо из бэклога «RSI-дивергенция (цена новый хай при падающем RSI)», фигурировал в «Следующее» несколько циклов.

**0 новых запросов** — переиспользует те же запечённые `spark[]`, что RSI/MACD/momentum. Новая `rsiArr(s,n)` строит полный ряд Wilder-RSI (не только последнее значение, как `rsi()`), `rsiDiv(s,14,5)` сравнивает ценовой экстремум последнего окна L=5 с предыдущим окном L=5: **bearish** = более высокий ценовой хай при более низком RSI-хае (моментум выдыхается у нового хая), **bullish** = более низкий ценовой лоу при более высоком RSI-лоу (продажи иссякают). Нужно ≥25 точек (n+2L+1), иначе «—». В таблицу добавлена колонка (▲ bullish зелёным / ▼ bearish красным, tooltip с определением), в note — список активных дивергенций.

**Зачем, если есть уровень RSI:** ограниченный осциллятор говорит «перекуплен/перепродан»; дивергенция ловит ослабление тренда ДО разворота уровня — цена ещё печатает новый хай, но осциллятор уже не подтверждает. Классический ранний сигнал разворота, ортогонален и уровню RSI, и MACD-кроссоверу.

**Данные на сборку (real-data self-check, Node-порт против запечённого `const TOKENS`, 18 CORE-токенов):** **STORM — bearish** (recentHigh 5.804e-3 > olderHigh 5.770e-3, но RSI 64.5 < 72.4 — определение подтверждено вручную), **GTA — bullish** (recentLow 2.404e-5 < olderLow 2.674e-5, но RSI 31.8 > 30.8), остальные — none. PASS: тип ∈ {bull,bear,null} для всех; монотонный рост → null; синтетический bullish → bull; scale-invariance подтверждена (×1e6 → тот же тип). Обе реальные детекции прошли определительную инварианту (HH&lowerRSI / LL&higherRSI).

**Блокеров по пути не было.** Правки внесены Edit-инструментом (Windows-side); целостность подтверждена ТОЛЬКО Read-tool'ом: `rsiArr`/`rsiDiv` (стр.840–859, по одному определению), `divCell`+колонка+note в `rsiGauge` (стр.871–893), `rsiGauge()` вызывается в analytics-boot (стр.545), `</script></body></html>` (стр.1245–1247) на месте. **Подтверждено снова:** bash-маунт QUANT усекает чтение/запись хвоста index.html — вся проверка целостности через Read-tool. git из песочницы НЕ запускался — деплой через `deploy.bat`.

Тесты: real-data self-check + 4 ассерта (PASS, см. выше). Тривиальная отрисовка (divCell по образцу zone) юнита не требует (conytail). API живые (TONAPI/STON.fi/DexScreener — 200).

Следующее: дивергенцию пометить и в MACD-карточке (цена новый хай при падающей гистограмме — уже в бэклоге); copy-trading (топ-кошельки TON через TONAPI) — главная money-фича; risk-return — фильтр «скрыть микрокапы <$X»; лид-лаг с лагом 2 дня.

## ✅ Готово 22.06.2026 (evolve-run, Opus, scheduled) — требует deploy.bat

**paper.html: «🔔 Today's signals — buy / skip» — мост research→action (карточка `#todayBody` сразу под KPI, самая верхняя).** Money-first: дашборд уже детектит сигналы и считает их edge, но не было экрана «что делать ПРЯМО сейчас». Карточка берёт сигналы последнего снапшота (`data/signals/{последняя дата}.json`) и аннотирует каждый его **conviction** — вердикт scoreboard'а (edge/neutral/noise/collecting) + walk-forward, превращённые в тот самый вес позиции, которым сайзится реальный бот. Сортировка по conviction. Метка действия: `buy` (≥0.6), `probe` (>0), `skip` (0). Conviction 0 = тип сигнала исторически теряет деньги → не гнаться.

**0 новых эндпоинтов** — те же JSON, что уже тянут другие карточки (signals/index.json, signals/{date}.json, scores.json), оба источника (локальный путь + GH raw fallback).

**Ключевое:** `conviction()` в JS — точный порт `load_score_mults` SIZE-ветки из paper.py (SHRINK=3, FULL=4, FLOOR=0.25): noise или x≤0 → 0; walk-forward-confirmed → 1.0; иначе excess, сжатый под малую выборку `x*n/(n+3)`, кап ≤1, пол 0.25. Так на экране ровно то, что бот реально заложил бы в размер.

**Данные на сборку (real-data self-check, Node-порт против настоящих scores.json+signals):** сегодня (2026-06-22) сработали momentum/HMSTR (+18%), accum_div/SCAT, liq_inflow/BOLT — **все conviction 0 (skip)**: momentum и accum_div — noise (отрицательный excess), liq_inflow — collecting с x=−2.1%. Карточка честно говорит «sit out, не гнаться за сырым пампом». Засветится зелёным когда сработает walk-forward-confirmed hidden_buyer (conv 1.0) или flow_imbalance (0.25). PASS: conviction∈[0,1] для всех; кросс-чек — JS conviction идентичен `load_score_mults` size paper.py по всем 6 типам (diff <1e-9); node --check синтаксис OK.

**Блокеров по пути не было.** Правки внесены Edit-инструментом (Windows-side); целостность подтверждена ТОЛЬКО Read-tool'ом: карточка (стр.50–56), IIFE (стр.400–439), `</script>/</body>/</html>` (стр.440–442) на месте. **Подтверждено снова:** bash-маунт усекает чтение cloud-only хвоста (cp scores.json дал 6347 байт битого JSON) — вся проверка через Read-tool; self-check гонялся на копии полного содержимого, восстановленного Read-tool'ом.

Тесты: real-data self-check + кросс-чек против paper.py (PASS, см. выше). Тривиальная отрисовка — без юнита (conytail). API живые (TONAPI/STON.fi/DexScreener — 200). git из песочницы НЕ запускался — деплой через `deploy.bat`.

Следующее: copy-trading (трекать топ-кошельки TON через TONAPI) — главная money-фича; conviction-метка в open positions paper.html; TON Quant Index — переключатель окна 7/30/all; risk-return — фильтр «скрыть микрокапы <$X».

## ✅ Готово 21.06.2026 (evolve-run, Opus, scheduled) — требует deploy.bat

**index.html: Risk-return map — размер пузыря = market cap (карточка `#rrCard`).** Снято прямо из бэклога «risk-return — размер точки = mcap» (фигурировал в «Следующее» у breadth и RSI).

**0 новых запросов** — модификация существующей карточки, переиспользует `mcapOf(t)` и запечённые `spark[]`. Радиус пузыря ∝ √mcap (значит ПЛОЩАДЬ ∝ mcap), клампится в [4,20]px: микрокапы сжимаются, крупные растут. Раньше все точки были r=5 — GTA ($28K mcap, неторгуемый) визуально весил столько же, сколько HMSTR ($34M). Теперь сразу видно, какие из высоко-Sharpe имён реально достаточно крупны/ликвидны, чтобы их торговать. Пузыри рисуются от крупных к мелким (мелкие сверху → кликабельны), полупрозрачны (overlap читается), tooltip получил mcap, подпись смещена на радиус. Добавлена «tradeable-edge линза» в note: медиана mcap = порог, лучший Sharpe среди крупной половины показывается отдельно, когда общий топ — микрокап («сильный Sharpe на микрокапе редко переживает слиппедж»). + легенда «bubble size = market cap».

**Данные на момент сборки (изолированный self-check на 18 CORE-токенах с запечённого spark, L≈40):** радиусы конечны и в [4.00, 20.00], монотонны по mcap (PASS); mcap-диапазон $28K (GTA) → $57.4M (STON); общий лучший Sharpe HMSTR 5.06 ($34M — сам крупный, поэтому линза не дублирует); медиана mcap $5.76M; лучший крупно-кап Sharpe тоже HMSTR (PASS — корректно выбран лучший среди ≥медианы). Обе ветви линзы (топ-крупный / топ-микрокап) проверены ассертами bestOfLarge — PASS.

**Блокеров по пути не было** — рабочее дерево пришло целым (git log чистый). Правки внесены Edit-инструментом (Windows-side), целостность подтверждена Read-tool после каждой записи: модификация `riskReturn()` (rows.map +`mc`, `rad()`, цикл пузырей, линза в note) на месте, `</html>` (стр.1198) на месте. (bash-маунт QUANT по-прежнему усекает чтение хвоста — `grep </html>` через bash пусто, реальный конец виден только Read-tool.) git из песочницы НЕ запускался — деплой через `deploy.bat`.

Тесты: real-data self-check на 18 живых spark (PASS, см. выше). Тривиальная √-математика сверх этого юнит-теста не требует (conytail). API живые (TONAPI/STON.fi/DexScreener — 200).

Следующее: momentum RS-стрелка тренда (RS сейчас vs неделю назад — ускоряется/тормозит); RSI-дивергенция (цена новый хай при падающем RSI); лид-лаг с лагом 2 дня; risk-return — фильтр «скрыть микрокапы <$X».

## Сейчас есть (v0.2, июнь 2026)

Главная: топ-20 по mcap (курируемый, без фейк-mcap типа TRAIN), live-цены, 7d-спарклайны, поиск по адресу, GRAM-переключатель. Дашборд токена: цена USD/TON(GRAM) + график 24h–90d, mcap, supply, холдеры + концентрация (донат, топ-25, top-10 %), DEX-ликвидность по всем пулам (STON.fi + DeDust через DexScreener), buys/sells 24h, риск-баннер (верификация, taxable, ликвидность, админ-ключ, кит-холдер), лента он-чейн событий.

## ✅ Выкачено 12.06.2026 (v0.3)

Tier 1 целиком: whale watch (фильтр ленты All / >$1K / >$10K с USD-оценкой каждого действия), кросс-DEX спред (только пары где токен — base, ликвидность >$5K, объём >$100), график цены USD/TON(GRAM), HHI с вердиктом, возраст токена (Trading since из DexScreener), FDV. Плюс из ресёрча: **TON Quant Score 0–100** (7 факторов, светофор, breakdown по hover), мульти-окна цены 5m/1h/6h/24h, buy/sell pressure бар, 🔥 trending movers на главной.

Следующее (утверждено): **bubble map холдеров** — лаконичная и читабельная; потребуется бесплатный TONAPI-ключ.

## ✅ Выкачено 13.06.2026 (evolve-run #6)

**Holder tier distribution на token.html (commit 006abba).** Карточка после таблицы топ-холдеров: stacked bar + легенда с вердиктом Whale-dominated / Mixed / Distributed. Тиры по USD-стоимости позиции: 🐋 Mega-whale ≥$100K / 🐳 Whale ≥$10K / 🐬 Dolphin ≥$1K / 🐟 Fish ≥$100 / 🦐 Shrimp <$100. Данные из уже загруженного holders.addresses (limit=100) — 0 новых запросов. Первый лайв-инсайт: MAJOR whale-dominated 72% (6 мега-китов 34.5% + 47 китов 37.7%) — hidden_buyer на этом фоне = не retail-аккумуляция, а доливка кита.

## Tier 1 — быстрые победы (API уже отдаёт, ~1–2 дня)

**Whale watch.** Фильтр ленты событий по крупным суммам (> $1K / $10K) с подсветкой — кто двигает токен прямо сейчас. Данные уже приходят в events.

**FDV vs Market Cap.** Если есть админ-ключ и supply не зафиксирован — показывать полностью разводнённую оценку и долю в обороте.

**Возраст токена и дата деплоя контракта.** Молодой контракт = риск-фактор; добавить в риск-баннер.

**Кросс-DEX спред.** Цена в разных пулах уже есть от DexScreener — показать разницу STON.fi vs DeDust в процентах. Арбитражникам полезно, никто на TON это не показывает.

**График цены в TON/GRAM** вторым режимом (тумблер на графике): мемкоины часто растут в USD, но падают к TON — это разоблачающая метрика.

**Индекс концентрации (HHI).** Одно число вместо «посмотри на донат»: рынок децентрализован / олигополия / один кит.

## Tier 2 — нужен сбор истории (GitHub Actions + JSON-снапшоты в репо, бесплатно)

**Динамика холдеров.** TONAPI не отдаёт историю — собирать самим: daily-снапшот топ-20 по cron в GitHub Actions, коммит JSON в репо. Через месяц будет график «холдеры растут/уходят» — главный сигнал жизни мемкоина.

**История ликвидности и объёмов.** Тот же механизм. «Ликвидность утекает 5 дней подряд» = ранний сигнал rug-а.

**Авто-обновление топ-20.** Тот же Action пересобирает рейтинг ежедневно — главная всегда актуальна без ручного деплоя.

**Метки кошельков (smart money).** Словарь известных адресов: биржи (OKX, Bybit, MEXC), фонды, мосты, деплоеры. «Кит #3 — это горячий кошелёк биржи» меняет интерпретацию концентрации на противоположную.

**Волатильность 30d и корреляция с TON/GRAM.** Считается из уже имеющихся графиков цены.

## Tier 3 — киллер-фичи (монетизация)

**TON Quant Score (0–100).** Композитный скоринг: верификация + ликвидность + концентрация + возраст + админ-ключ + динамика холдеров. Бейдж на каждой карточке. Это самоцитируемая фича — её будут скринить и постить.

**Telegram-алерты.** Цена пробила X / кит купил / ликвидность ушла на Y%. Инфраструктура Telegram-бота уже есть (igor-workbench). Free: 1 алерт, Pro: безлимит. Прямой путь к подписке.

**TON Quant как Telegram Mini App (дистрибуционный трек, 13.06.2026).** НЕ источник edge — канал доставки/исполнения, но самый сильный для нашей аудитории: вся аналитика + Alpha digest + алерты внутри Telegram, вход по TON Connect (кошелёк). Сажает продукт туда, где живут TON-дегены, и превращает алерты в интерактивный дашборд. Тот же TON Connect + STON.fi/DeDust SDK внутри мини-аппа — штатный путь исполнения для будущего бота (фаза «реальные деньги/ключи»). Осторожно: масса Telegram trading/sniper-ботов — комиссионные пиявки/скам, не роутиться через них. Мини-аппы новой альфы НЕ дают.

**AI deep-research отчёт по токену.** Кнопка «Generate report» → структурированный отчёт (фундаментал, он-чейн, риски, сравнение с сектором) — изначальная идея монетизации TON Quant.

**Портфель по адресу кошелька.** Вставь свой адрес → все жетоны, стоимость, PnL. TONAPI отдаёт балансы кошелька одним запросом.

**Сравнение токенов side-by-side.** 2–3 токена в колонках: для «что купить из мемов» это убийца вкладок.

## 💰 Alpha Engine — путь к торгующему ИИ-боту

Цель Игоря: сигналы → деньги → автономный бот. Путь по этапам, каждый проверяем перед следующим:

**Этап 1 — сигналы на сайте.** ✅ Готово (v0.5): Alpha digest на главной — momentum, wash, absorption, hidden buyer, арбитраж, перегрев по всем 24 токенам в реальном времени.

**Этап 2 — журнал сигналов.** ✅ Готово (12.06.2026): paper.py в daily Action пишет `data/signals/YYYY-MM-DD.json` (addr, sym, тип, вес, цена в момент сигнала, tvl, vol24, holders, d1) + `data/signals/index.json` (даты, идемпотентно) + список wash-banned за день. Тест: `tests/paper.test.py` (13 проверок: 6 однодневных сигналов на синтетике, схема записи, wash-исключение, идемпотентность). Цена в момент сигнала — фундамент Этапа 3: исходы 1д/3д/7д считаем по снапшотам, когда журналу ≥7 дней (~19.06).

**Этап 3 — бэктест и скоринг.** ✅ Инфраструктура готова (12.06.2026, evolve-run #2): `scripts/score.py` в daily Action после paper.py — чистый пересчёт журнала: для каждого события сигнала исход = сырое движение цены через +1д/+3д/+7д (по снапшотам, пропуск дня = null), агрегаты per signal (n, win-rate, avg, median) → `data/signals/scores.json` (идемпотентно, + 30 последних событий). Вердикт по 3д-горизонту при n≥5: edge (WR≥55% и avg>0) / noise (WR<45% или avg<0) / neutral / collecting. UI: карточка «Signal scoreboard» на paper.html (1д/3д/7д WR+avg, вердикт-пилюли). Тест: `tests/score.test.py` (16 проверок: математика исходов, gap в снапшотах, агрегация, вердикты, идемпотентность). Workflow проверен ручным dispatch — зелёный. Первые исходы ~14.06, вердикты с n≥5. Когда журналу 2-4 недели — выясним, какие сигналы РЕАЛЬНО предсказывают (гипотезы: hidden buyer и аккумуляция холдеров — да; momentum на тонких мемах — слабо), и присвоим веса по win-rate. Это и есть «edge» — без него бот будет просто сжигать деньги.

**Лайв unrealized P&L в paper.html. ✅ ЗАДЕПЛОЕНО 2026-06-18 (evolve-run #18, commit 699624d5):** paper.html open positions — добавлены столбцы «Live Δ» и «Unr. PnL». После рендера позиций фетчит текущие цены через TONAPI batch rates, вычисляет unrealized return (cur/entry_eff−1) и Unr. PnL ($size × return), цветовая подсветка. Починены integration tests: arb→activityDigest, whaleTape→renderTape. 87/87 unit, 23/26 integration.

**Этап 4.2 — impact-aware сайзинг (12.06.2026, фича #4). ✅ ЗАДЕПЛОЕНО 13.06.2026 (evolve-run #5):** `scripts/paper.py` (202 стр., 19/19 тестов) закоммичен в репо (13.06 20:12 UTC), daily Action со след. прогона (14.06 03:10 UTC) сайзит позиции по глубине пула. `paper.py`: фиксированный сайз $100/$150 заменён на `position_size(cfg,t)` — размер позиции = min(base, impact-cap) × vol-scale, где impact-cap = глубина пула под ≤1% CPMM-импакт ((tvl/2)·(√1.01−1)), а vol-scale понижает сайз при высоком turnover (vol24/tvl — flow-implied vol по Конту; ref 0.5, пол 0.3). Ключевое свойство: сайз НИКОГДА не превышает base → риск только равен или ниже прежнего. Глубокие пулы держат полный base, тонкие — режутся (пример: пул $25K, turnover 0.8 → $38.97 вместо $150). Позиции и сделки теперь несут поле `size`; PnL считается от фактического size. Бэк-совместимость: старые позиции без size → fallback на cfg["pos"]. Тест `tests/paper.test.py` расширен (+токен F тонкий-пул + 6 проверок: size-поле, ≤base, ≤impact-cap, F режется <$100, A держит $100): 19/19. Следующее по плану: #1 (динамические веса сигналов из score.py, когда журналу ≥7 дней) > #3 (out-of-sample) > #10 (toxicity-фильтр).
**Этап 4.1 — ДВА бота (v2, 12.06.2026).** ✅ По ресёрчу академической литературы (краткосрочная реверсия работает в мелких/неликвидных криптомонетах, моментум — в крупных; double-sort ~20%/год; breakout на объёме 2x; liquidity ratio как фильтр стабильности) сигнальный набор расширен до 8: hidden_buyer, holders_surge (+1.5%/день), accum_div (цена ↓3% при росте холдеров), liq_inflow (TVL +20% при флэте), momentum (>8%), breakout (цена выше max 7д + объём 2x среднего), dip_reversal (−12% на качественном токене — реверсия), flow_imbalance (buys ≥65% при ≥80 сделках). 🛡️ **Консервативный**: только аккумуляционные сигналы + фильтры качества (TVL≥$50K, холдеры≥5K, liq/mcap≥2%), $100×3, +15/−7/5д. ⚡ **Агрессивный**: все 8 сигналов, $150×6, +30/−12/3д. Оба: rug_exit при TVL −25%/день (на синтетическом тесте порезал убыток с −100% до −47%), wash-бан 7 дней, честные издержки. Сравнение на paper.html.

**Этап 4 — paper-trading бот.** ✅ Запущен 12.06.2026 (решение Игоря: руками не торгуем, сразу паперка). `scripts/paper.py` в daily Action: виртуальные $1,000, позиции по $100 (макс 5), вход по сигналам hidden_buyer (приоритет) и momentum >8%, честные издержки — CPMM-слиппедж от глубины пула + 0.3% комиссия на каждую ногу, выход +20% тейк / −10% стоп / 3 дня таймаут, wash-токены в бан на 7 дней. Дашборд: **paper.html** — equity curve, win-rate ПО ТИПАМ сигналов (эта таблица и решит, чем будет торговать реальный бот). Первые сделки — со второго снапшота (сигналам нужна вчерашняя цена).

**Этап 5 — Telegram-алерты.** Опционально, параллельно паперке: пуш сигналов, которые паперка уже подтвердила положительным win-rate.

**Про арбитраж сейчас:** ✅ **Net-arb radar выкачен 12.06.2026 (evolve-run #3).** Арбитражный радар на главной теперь ранжирует не по gross-спреду, а по РЕАЛИЗУЕМОЙ чистой прибыли: `netArb()` сканирует размеры сделки (до 50% резерва тонкого пула), вычитает CPMM-слиппедж обеих ног (≈S/(liq/2)), 0.6% комиссий и ~0.3 TON газа, и берёт размер, максимизирующий net-$. Профитные строки — зелёные «+$X (Y%)» с оптимальным сайзом, остальные — «unprofitable». Alpha digest триггерит арб-сигнал только при net>0. Подтверждено на лайве: из 9 multi-pool токенов профитен лишь DOGS (+$1 @ $77, 0.81% net при 3.49% gross); tsTON/DUST/STON/GRM/NOT (gross 0.7–1.1%) — всё съедается издержками. Тесты: +8 unit (netArb матчит независимый ref, монотонность по спреду и глубине, кэп сайза, zero/thin → unprofitable), integration обновлён под новый радар (27/27 на лайве). Видеть спреды и считать чистый арб (после слиппеджа двух ног, комиссий и газа) можем уже сегодня — математика вся есть.

**Газ перекалиброван (13.06.2026).** `netArb()` газ 0.3 → 0.05 TON round-trip: 0.3 TON было «прикрепляемым бюджетом» STON.fi (неизрасходованное возвращается), а не сожжённой стоимостью; реальный своп ~$0.03, после ~6x-среза сети TON (Catchain 2.0, апр-2025, фикс ~$0.0005/tx) — ещё ниже, круг ~$0.02–0.05. Эффект на лайве: профитных строк стало 3 (UTYA/DOGS/GRM) вместо 1 — газ был binding на малом сайзе. НО главный порог — pool-fee 0.6% round-trip (комиссия DEX, НЕ сети, не менялась) + слиппедж: профиты всё равно копеечные ($0–1), тонкие спреды <~1% по-прежнему съедаются. paper.py сетевой газ не моделирует — срез это узаконивает (~0.02% от позиции $100). Идеал: замерить газ по реальной tx, а не держать константой. Исполнять сделки в DEX может только сам Игорь или его будущий бот с кошельком (STON.fi SDK / DeDust SDK). Реалистично: спред <2% на тонких пулах почти всегда съедается издержками; кандидат в paper-стратегию №3 — «арб» с полным расчётом net-profit, добавим после первых данных паперки.

**Этап 6 — реальный бот.** Только после доказанного paper-PnL. Особенности TON: тонкая ликвидность (наш порог входа — суммы, влезающие в <1% импакта), taxable-жетоны, газ. Исполнение через STON.fi SDK.

Риски, которые держим в голове: на тонких мемах сигналы частично создаются манипуляторами (wash, спуфинг) — поэтому скоринг по историческим исходам обязателен; прошлый win-rate не гарантирует будущий; размер позиции всегда ограничен ликвидностью.

## ✅ Выкачено 14.06.2026 (evolve-run #8)

**Snapshot divergence signals в Activity Digest на главной (commit b4b89db).** `snapSignalFor(a,b)` — чистая функция: по двум снапшотным записям токена возвращает вид сигнала или null. Виды: `rug_watch` (TVL < −20%), `accum` (holders > +1.5% при цене < −2%), `distrib` (holders < −1% при цене > +5%), `liq_inflow` (TVL > +30%). `loadSnapshots()` в boot: фетчит `data/index.json` + последние 2 снапшота, заполняет `snapSigs[]`, вызывает `activityDigest()`. Сигналы рендерятся в Activity tape с меткой «1d snap» вместо vol. Лайв 14.06: **stTON ⚠️ Rug watch — TVL −39.6% за 24ч**  (реально — отток ликвидности из staked-пула), GROYP +12.1% цена, COIN −14.7% −3.3% холдеры. 7 новых unit-тестов (57/57). Обновлён unit.test.mjs: grab() переписан под текущий index.html (netArb/pearson/rets/mxColor/esc2 → стабы/рефимплы). ТЕХ: OneDrive-маунт снова надёжен для Read/Edit; bash-граб из деплой-папки работает; unit-тест в QUANT папке теперь требует `TQ_IDX=deploy/index.html TQ_TOK=deploy/token.html`.

## ✅ Готово 16.06.2026 (evolve-run #12, scheduled) — deploy/ ждёт git push

**1. token.html: тёмная тема.** Удалён инлайн `:root { color-scheme:light; ... }` — styles.css dark theme теперь работает. Починены хардкодные светлые цвета: `.switch` `#d6ddea`→`var(--line)`, `.banner.warn/good/bad` border/color→`var(--warn/good/bad)`, `.chip` `#eef0f3`→`var(--line)`, `.pill.warn/bad` hex-цвета→`var(--warn/bad)`, `.pill.muted/burn` `#eef0f3`→`var(--line)`, `.pill.exchange/bridge` → `rgba`-dark, `.ev-tag.JettonTransfer/TonTransfer/ContractDeploy/SmartContractExec` → dark-совместимые rgba+var. Файл 984 строки. **2. Multi-day rug radar в index.html.** `loadSnapshots()` переписан: теперь фетчит ВСЕ снапшоты (Promise.all всех дат из index.json), строит per-token историю `hist[]`, вызывает `persistentSignal(arr)` для токенов с ≥3 снапшотами. Новая `persistentSignal()`: `rug_confirmed` (TVL −10%/d каждый из 2 дней И итого −20% — это уже не шум), `holder_exodus` (холдеры −0.5%/d × 2д итого −1%), `accum_confirmed` (холдеры +0.5%/d × 2д при падающей цене). Сигналы получают бейдж «2d snap» и приоритет выше однодневных (2e6/1.5e6/1.2e6 vs 1e6). Однодневный `rug_watch` применяется только если у токена нет мультидневного сигнала (usedAddr.Set). Эффект: stTON-rug_watch 2 дня подряд → следующий раз будет `🚨 Rug confirmed`. Тесты: 65/65 unit (+8 persistentSignal-тестов).

## ✅ Выкачено 14.06.2026 (evolve-run #9)

**screener.html: полный фикс + snapshot divergence signals (commit 85c29d8).** Страница была сломана с reskin'а: светлая тема (инлайн `:root` override), битые nav-ссылки (`main-page.html`→`index.html`), хардкод светлых цветов в `.pill.warn/.pill.bad/ev-tag`. Всё починено. Добавлена колонка «Snap» (colspan 10→11) и дропдаун-фильтр по снапшот-сигналам. Перенесены `snapSignalFor(a,b)` + `loadSnapshots()` из index.html; стартуют в `boot()` параллельно с `loadPrices()`/`loadDex()`. `alphaDigest()` мержит snap-сигналы с приоритетом (rug_watch=2e6, accum=8e5, distrib=6e5, liq_inflow=4e5). `resetFilters()` сбрасывает snap-фильтр. Пилюля — строковая конкатенация (не вложенный template literal — баг Python `\`` в JS). ДАННЫЕ РЕАЛЬНЫЕ: **stTON ⚠️ Rug watch TVL −39.6% второй день подряд** — персистентный слив ликвидности; **REDO +$12 арб (11.6% gross / 5.69% net)** — крупнейший профитный арб; MTONGA +12.72% (импульс); GRM −14.07%. Тесты: 57/57 unit, 26/26 integration.

## ✅ Выкачено 15.06.2026 (evolve-run #11)

**token.html: тёмная тема + holder tier distribution (deploy/ готов, требует git push).**
1. Удалён инлайн `:root { color-scheme: light; ... }` → оставлен только `--nav-h:56px`; тёмная тема из styles.css теперь работает на token.html (та же правка, что в run #10 для paper/compare/portfolio). Починены хардкодные цвета: `.banner.warn/good/bad`, `.pill.warn/bad`, `.ev-tag.SmartContractExec` → `var(--warn/--good/--bad)`. Nav-ссылки `main-page.html` → `index.html`.
2. Re-добавлена **Holder tier distribution** карточка (была в run #6, потеряна при reskin): после Top holders, скрытая (`display:none`) пока не загрузятся данные. Тиры по USD-стоимости позиции: 🐋 Mega-whale ≥$100K / 🐳 Whale ≥$10K / 🐬 Dolphin ≥$1K / 🐟 Fish ≥$100 / 🦐 Shrimp <$100. Stacked bar + легенда + вердикт (Whale-dominated/Mixed/Distributed) из уже загруженных holders.addresses — 0 новых запросов. Тесты: 57/57 unit. Integration test не прошёл из-за bash-mount truncation (файл 1032 строк через Read-tool, 983 через bash — известный баг); код корректен.

## ✅ Готово 16.06.2026 (evolve-run #13, scheduled) — deploy/ ждёт git push

**token.html: Price × Holder history card (re-add run #7 feature, lost in reskin).** Карточка `#snapHistCard` между Top holders и Recent activity. `snapDivLabel(dh,dp)` → 🟡 Accum / 🟢 Momentum / 🔴 Distrib / 🔴 Exit / ⚪ Stable (пороги dh=±0.1%, dp=±0.1%). `loadHolderHistory()` async: фетчит `data/index.json` → все снапшоты (Promise.all) → per-token deltas → рендерит таблицу + multi-day verdict (Rug Confirmed / Holder Exodus / Accum Confirmed из логики persistentSignal прямо в функции). Badge показывает кол-во дней истории. Тесты: 77/77 unit (+12 snapDivLabel-тестов из tok, sandbox получил `location.replace:()=>{}`). Файл 1081 строка. Снапшотов 5 (12-16.06) → первые 4 строки в таблице. Сигналы 16.06: UTYA/accum_div, FRT/flow_imbalance, GRM/dip_reversal, SCAT/accum_div, BOLT/liq_inflow. Скоры (9 событий): hidden_buyer h1 WR=50% avg=−0.08%, momentum h1 WR=50% avg=−2.81% (обе "collecting"). ⚠️ ВАЖНО для следующего evolve (~19-20.06, ≥7 снапшотов): динамические веса сигналов из scores.json (фича #1 бэклога, веса по win-rate).

## ✅ Выкачено 16.06.2026 (evolve-run #15, scheduled) — commits 279ecc8 + bc4e154

**Массовый деплой + paper.html: Recent Signal Outcomes table.**
Runs #11–14 задеплоены: index.html (alpha signals в Activity Digest), token.html (тёмная тема + holder tier + price×holder history card). Новое на paper.html: секция «📋 Recent signal events» — хронологическая таблица всех событий из `scores.json.recent` с r1/r3/r7, автометка `fade` (badge «fade» когда r1>2% но r3<-5%). Первый сигнал fade-паттерна на реальных данных: momentum Yoda h1=+5.23% → h3=−25.41% — красный флаг для momentum-стратегии на тонких TON мемах.

## ✅ Готово 16.06.2026 (evolve-run #14, scheduled) — ЗАДЕПЛОЕНО в run #15

**index.html: Alpha Bot signals в Activity Digest.** `loadAlphaSignals()` — новая async функция: фетчит `data/signals/scores.json` (per-signal статистика WR/avg) и `data/signals/YYYY-MM-DD.json` (сигналы дня). Новый хелпер `sigEdgeStr(sc)` — читаемая строка из scores: "collecting" (n<3), "n=X WR Y%" (n<5), "edge WR=X% avg=+Y%" / "noise WR=X%" / "neutral WR=X%" (n≥5). `SIG_EMOJI` маппинг: hidden_buyer🐋, momentum🚀, accum_div📊, flow_imbalance🌊, dip_reversal🔄, liq_inflow💧. Приоритет = w·1e5 (edgeBoost×1.5 при avg>0 и n≥3). Сигналы рендерятся в Activity tape с меткой «alpha bot», показывают вес + 1d% + edge-строку. Тесты: 85/85 unit (+10 sigEdgeStr/SIG_EMOJI-тестов). Сигналы 16.06: UTYA/accum_div, FRT/flow_imbalance, GRM/dip_reversal, SCAT/accum_div, BOLT/liq_inflow. ⚠️ Динамические веса (~19-20.06, ≥7 снапшотов) — следующий приоритет.

## ✅ Готово 17.06.2026 (evolve-run #16, scheduled) — deploy/scripts/ ждёт git push

**Alpha Engine: динамические веса сигналов (фича #1 бэклога).**

1. **`scripts/score.py`**: `MIN_N` снижен 5→3 — теперь вердикты выдаются при ≥3 событиях на горизонте. При следующем Action-прогоне (18.06 03:10 UTC): `accum_div` (h1 n=3, WR=0%, avg=−6.4%) → **noise**, `momentum` (h1 n=4, WR=50%, avg=−2.8%) → **noise** (avg<0). Первое доказательство fade-паттерна на TON мемах подтверждено данными.
2. **`scripts/paper.py`**: добавлена `load_score_mults()` — читает `data/signals/scores.json` → `per_sig` → возвращает `{sig: mult}` по вердикту. `SCORE_MULTS = {edge:1.5, neutral:0.8, noise:0.3, collecting:1.0}`. Применяется в `run_bot()` при ранжировании кандидатов: `cands.append((w * mult, ...))`. Журнал (scores.json) записывает raw w без изменений — только приоритет входа корректируется. Итог: как только Action пересчитает scores.json с MIN_N=3, боты перестанут предпочитать momentum и accum_div — самые убыточные сигналы по данным.
3. Тесты: 19/19 paper.test.py, 16/16 score.test.py. Файл 219 строк.

Данные сегодня: 6 снапшотов (12-17.06), 14 событий в scores.json. Сигнал дня (17.06): GTA/momentum (+?%). ДАННЫЕ: accum_div h1 WR=0% avg=−6.4% (все 3 события отрицательные) — статистически нет ни одного выигрышного входа; momentum h3 WR=0% avg=−15.2% (Yoda −25.4%, GROYP −5.0%) — fade к 3д подтверждён. hidden_buyer h3 WR=100% avg=+2.3% (n=2) — пока лучший сигнал на 3д-горизонте.

## ✅ Готово 18.06.2026 (evolve-run #19, scheduled) — ton-quant/paper.html готов, требует deploy.bat

**paper.html: Buy&Hold benchmark на equity chart (фича #2 бэклога).**

Новая async `loadBenchmark()`: фетчит `data/index.json` (даты) + `data/cats.json` (категории) + все снапшоты (Promise.all) → вычисляет equal-weight return CORE-токенов (не stable, не staking) относительно первого дня → массив `[{d, v}]` с базой $1000. `drawEq(bots, bench)` расширен: принимает опциональный `bench` и рисует серую пунктирную линию (strokeStyle #555c6e, lineDash [6,4]). Легенда карточки обновлена: добавлен «⸻ Buy&Hold». KPI-карточка «Buy&Hold (CORE)» добавляется динамически справа от бот-KPI, если benchmark загрузился.

**Ключевые данные (18.06, 7 снапшотов):** всё в красной зоне — hidden_buyer verdict=**noise** (h1 n=4, WR=50% avg=−0.08%), momentum verdict=**noise** (h1 n=5, WR=40% avg=−2.36%, h3 WR=0% avg=−13.11%). Бот-статус: cons $989.63 (−1.04%), aggr $869.41 (−13.06%). Сигналы дня: STON + MAJOR (оба hidden_buyer, w=3.0, подавлены ×0.3 → eff. 0.9). Benchmark покажет, сколько пассивный hold принёс — если бота обгоняет, ключевой аргумент пересмотреть стратегию.

Тесты: 87/87 unit (старые), 8/8 bench_unit (новые — computeBenchmark: stable цены=1000, stables excluded, one-doubles-one-halves, missing token=ratio-1, no core→null, structure+scale).

## ✅ Готово 19.06.2026 (evolve-run #22, Opus, scheduled) — требует deploy.bat

**Market-relative (excess) returns + walk-forward OOS + статзначимость в `scripts/score.py` (фича #3 бэклога — главное незакрытое).**

Проблема, которую это решает: на АБСОЛЮТНЫХ доходностях весь рынок падает (TON + большинство джеттонов в красном) → каждый сигнал штампуется как "noise" (см. run #19: hidden_buyer и momentum оба noise). Но edge есть, только если сигнал бьёт корзину. Поэтому переписан `score.py`:
- **Excess return** = доходность сигнала − средняя доходность торгуемой корзины (CORE, без stable/staking) на том же горизонте. Win-rate/avg/median считаются на excess, не на абсолюте.
- **Wilson 95% нижняя граница** на excess win-rate + двусторонний **знаковый тест** (точный биномиальный p). Вердикт «edge» теперь требует Wilson lo > 50% (статзначимо выше монетки), а не просто точечную оценку — это убивает ложные edge на малом n.
- **Walk-forward**: даты делятся пополам, вердикт формируется на in-sample, OOS подтверждение (`confirmed` флаг) отдельно. С n=15 это честно показывает, держится ли паттерн вне выборки.
- Стат-хелперы (`wilson_lo`, `sign_test_p`, `agg_returns`, `classify`) вынесены чистыми и importable → `scripts/score_test.py` (22 проверки, все зелёные). Бэк-совместимость: абсолютные h1/h3/h7 и ключ `verdict` сохранены (paper.py читает только verdict; index.html — h1, есть legacy-fallback).
- `index.html`: `sigEdgeStr` теперь показывает вердикт + excess avg + Wilson lo + ✓oos (с legacy-fallback на старый формат scores.json); ранжирование `loadAlphaSignals` буст ×2.0 для confirmed-edge, ×0.5 для noise.

**💰 КЛЮЧЕВАЯ НАХОДКА (8 снапшотов, 15 событий):** `hidden_buyer` на excess-доходностях = **EDGE, walk-forward CONFIRMED**. Excess +7.7%/3д (n=4, WR=100%, Wilson lo 51%, sign-test p=0.125), in-sample +1.1% → out-of-sample +5.5%. То есть когда рынок падал ~6%, токены с hidden_buyer падали лишь ~0.4% → +7.7% относительной альфы, и это держалось вне выборки. Это первый сигнал с market-relative edge, переживший OOS. ⚠️ n=4, p=0.125 — многообещающе, но НЕ доказано (нужно p<0.05, т.е. ≥7-8 подряд excess-выигрышей). momentum остаётся noise (excess h3 −5.1%, OOS −1.26%, не confirmed). Это relative-value сетап под market-neutral бот: лонг hidden_buyer / шорт корзины.

Тесты: 22/22 score_test.py, 57/57 unit (sigEdgeStr провалидирован в изоляции на реальном scores.json: edge+✓oos для hidden_buyer, noise для momentum, legacy-fallback, null). 3 фейла integration — блок NOT в token.html (живой fetch холдеров tonapi в песочнице вернул пусто), token.html не трогался, не регресс. scores.json в репо НЕ коммитим (origin уже имеет сегодняшний CI-коммит, избегаем rebase-конфликт) — новый формат сгенерит следующий daily Action (или ручной workflow_dispatch «Daily snapshot»).

Следующее: добить hidden_buyer до p<0.05 (нужны дни), CVD/toxicity-фильтр (#10), динамические веса по excess-WR в paper.py.

## ✅ Готово 19.06.2026 (evolve-run #23, Opus, scheduled) — требует deploy.bat

**index.html: Correlation matrix + β to basket (фича #10 бэклога — «вау, уникально для TON»).**

Новая карточка `#corrCard` после Market map. Чистая клиентская математика, **0 новых запросов** — переиспользует уже запечённые `spark[]` (40 дневных закрытий на токен из `snapshot.py`). Берёт топ-12 CORE-токенов (без stable/staking) с валидным спарком, выравнивает по общей длине, считает лог-доходности и попарную корреляцию Пирсона. Тепловая матрица: зелёный = ходят вместе, красный = противоход. Последняя колонка — β к equal-weight корзине (рыночный фактор из средней доходности отобранных токенов): >1 усиливает рынок, <0.5 приглушён, <0 хедж. Внизу авто-вывод: самая скоррелированная пара + лучший диверсификатор (мин. средняя корреляция).

Новые чистые функции: `lrets` (лог-доходности, пропускает неположительные), `pcorr` (Пирсон, null при n<5), `betaVs` (cov/var к рынку), `corrColor` (диверг. шкала). Вызывается раз на boot (`treemap(); correlMatrix()`) — спарк статичен, в 60-секундный refresh не входит.

**Данные на момент сборки (40 закрытий):** самая скоррелированная пара **STON–DUST r=0.95** (оба DeFi/DEX-токены ходят синхронно — кластер), лучший диверсификатор **HMSTR avg r=0.02** (Hamster Kombat живёт своей жизнью — реальная диверсификация в мем-корзине). Это первый на TON взгляд «весь рынок одним кадром по кластерам и бете».

**Баг-фикс:** `integration.test.mjs` — ассерт «NOT holders match» сравнивал два независимых live-фетча holders_count на точное равенство; реальный счётчик тикает между запросами (флак 2,848,072 vs 2,848,070). Заменено на допуск ±max(200, 0.01%). Коренная причина, не симптом.

**Проверка API:** TONAPI (`/v2/rates`, `/v2/jettons`, `/v2/accounts`, `/v2/rates/chart`), STON.fi (`/v1/assets`), DexScreener, CoinGecko — все живые, эндпоинты актуальны. Реберндинг TON→GRAM не ломает код: DexScreener `chainId` остался `"ton"`, ключ rates остался `TON`, quote-символ теперь рендерится как GRAM (корректно).

Тесты: новая корреляция/бета провалидирована end-to-end на реальном TOKENS (7/7: card shown, матрица рендерится, ячейки окрашены, β-колонка, диагональ=1.00, вывод-нота, все r∈[-1,1]); +9 unit-ассертов (pcorr self/anti/null, betaVs self/2x, lrets, wiring-guards). ⚠️ Полный node-прогон unit/integration в песочнице заблокирован известным багом QUANT-маунта (bash-копия index.html усечена по хвосту на запись; Windows-файл через Read-tool целый и корректный — git на машине Игоря закоммитит правильный рабочий-три). Поэтому git-commit из песочницы НЕ запускался (закоммитил бы усечённый блоб) — деплой строго через `deploy.bat` на Windows.

Следующее: добить hidden_buyer до p<0.05 (нужны дни), CVD/toxicity-фильтр, динамические веса по excess-WR в paper.py, лид-лаг радар (надстройка над этой корреляцией — кросс-корр со сдвигом).

## ✅ Готово 20.06.2026 (evolve-run #24, Opus, scheduled) — требует deploy.bat

**index.html: Lead–lag radar (лид-лаг радар — надстройка над corrCard, явно следующий пункт бэклога).**

Новая карточка `#llCard` после Correlation matrix. **0 новых запросов** — переиспользует те же запечённые `spark[]` (40 дневных закрытий), что и corrCard. Для каждой пары X→Y считает корреляцию доходности X в день t с доходностью Y в день t+1 (лаг 1). X «ведёт» Y, когда forward-корреляция сильная (≥0.3) И превышает обратную Y→X (+0.1) — неформальный Granger-тест направления на дневных закрытиях. Таблица топ-6 пар (lead r / rev r) + подсветка corrColor. Ниже — net-leadership рейтинг: кто чаще ведёт корзину (сумма fwd−rev по всем парам).

Новые чистые функции: `laggedCorr(a,b,k)` (переиспользует `pcorr` на сдвинутых срезах), `leadLag()`. Вызывается раз на boot (`correlMatrix(); leadLag();`) — spark статичен, в 60-сек refresh не входит.

**Данные на момент сборки (40 закрытий, 12 CORE-токенов):** 2 направленные пары выше порога — **UTYA ведёт FRT** (lead r=0.34 vs rev 0.06) и **NOT ведёт FRT** (0.32 vs 0.19). Сильнейший лидер **UTYA** (net +2.6), биггест фолловер **DUST** (net −1.6). FRT (FORT, DeFi) систематично реагирует на движения UTYA/NOT с днёвным лагом — потенциальный сетап «лидер дёрнулся → ждём фолловера». ⚠️ дневная гранулярность + n=39 → подсказка, не доказательство (в UI помечено явно).

**Блокер по пути (починен):** рабочее дерево было повреждено mount-truncation багом предыдущего прогона — index.html/ROADMAP.md/unit.test.mjs/integration.test.mjs обрезаны по хвосту (index 66827→обрыв на середине loadAlphaSignals, ROADMAP обрыв на слове «п»). deploy.bat делает `git add -A && commit && push` — без фикса Игорь закоммитил бы битые файлы в live. Восстановлены из целого HEAD через `git show HEAD:f > f` (bash-redirect надёжен, проверено побайтово). **ВАЖНО для следующих прогонов:** Edit/Write-инструменты (Windows-side) на этом репо усекают файлы при записи; надёжный путь — только bash-redirect (`cat /tmp/f > f`), каждая запись с read-back проверкой размера/хвоста.

Тесты: 90/90 unit (вкл. +4 новых: laggedCorr perfect-lag=1, directional fwd>rev, leadLag defined, called-at-boot). Real-data self-check прогнан на живых spark (12 токенов, L=40): все r∈[-1,1], net конечен, вывод невырожденный. API живые (TONAPI/STON.fi/DexScreener — 200).

Следующее: лид-лаг с лагом 2 дня / разными горизонтами; добить hidden_buyer до p<0.05 (нужны дни); динамические веса по excess-WR в paper.py.

## ✅ Готово 20.06.2026 (evolve-run #25, Opus, scheduled) — требует deploy.bat

**index.html: Risk–return map — Sharpe-рейтинг (фича из Tier 2 «волатильность 30d», но в рыночном разрезе всей корзины).**

Новая карточка `#rrCard` после Lead–lag radar. **0 новых запросов** — те же запечённые `spark[]` (~40 дневных закрытий). Для каждого non-stable (CORE) токена: дневные лог-доходности → среднее и популяционное std → аннуализация (rf=0): return = mean×365, vol = std×√365, **Sharpe = mean/std×√365**. SVG-скаттер (inline, без либ): X = годовая волатильность, Y = годовая доходность, цвет точки = Sharpe (зелёный хорошо / красный плохо), пунктир на нуле доходности. Ниже — рейтинг топ-8 по Sharpe (return / vol / Sharpe) и авто-вывод: лучший risk-adjusted + худший. «Выше и левее = больше награды на единицу риска» — классический квант-взгляд, которого на TON никто не показывает.

Новые чистые функции: `dstats(r)` (mean+pop std), `sharpe(r)` (null при std=0). Переиспользует `lrets`, `corrColor`, `esc`. Вызывается раз в конце `loadDex().then` (`leadLag(); riskReturn();`) — спарк статичен, в refresh не входит.

**Данные на момент сборки (40 закрытий):** HMSTR — сильнейший risk-adjusted (Sharpe ≈5.1: +809% годовых при 160% vol, ралли Hamster), MAJOR — худший (Sharpe ≈−4.1: −280% годовых), NOT почти нейтрален (Sharpe ≈0.2). Короткое окно → числа крупные (аннуализация 40 дней), в UI явно помечено «snapshot, не прогноз».

**Блокер по пути (обнаружен, обойдён — ВАЖНО для следующих прогонов):** bash-маунт QUANT усекает не только запись, но и **чтение** — `cat`/`wc`/`git show`/`cp` через маунт отдают обрезанный по хвосту файл (index.html: bash видит 841 строк без `</html>`, реальный файл 862 строки; unit.test.mjs bash видит 242 строки, реально 249). Поэтому bash-сравнения размеров «совпадают» ложно (две обрезанные копии равны) и cp-в-/tmp + патч-скрипт дают усечённый результат — писать его обратно нельзя. **Надёжный путь: Read/Edit-инструменты (Windows-side) — читают и пишут целиком.** Эта правка сделана через Edit, целостность хвоста (`</html>`, 917 строк) подтверждена Read-tool'ом после каждой записи. Self-check математики прогнан изолированно в песочнице на реальных spark (HMSTR/MAJOR/NOT): все значения конечны, знаки и порядок Sharpe верны — PASS.

Тесты: +8 unit (dstats mean/std=√2/null, sharpe positive/null-at-std0, riskReturn defined/called-at-boot/uses-CORE). Полный node-прогон сьютов из песочницы невозможен (тест читает усечённый index.html через маунт → ломается парс `<script>`); вместо него — изолированный real-data self-check (PASS) + Read-tool верификация целостности файлов.

Следующее: лид-лаг с лагом 2 дня; risk-return — добавить размер точки = mcap; добить hidden_buyer до p<0.05; динамические веса по excess-WR в paper.py.

## Техдолг / инфраструктура

OG-картинки для шеринга карточек токенов.

---
*Обновлено: 2026-06-20. Блокчейн остаётся «TON», монета с 15.06.2026 — GRAM (вотум 81% за).*

## ✅ Готово 20.06.2026 (evolve-run #25, Opus, scheduled) — требует deploy.bat

**index.html: Drawdown radar (карточка `#ddCard` после Risk–return map).**

**0 новых запросов** — переиспользует те же запечённые `spark[]` (~40 дневных закрытий), что и corr/lead-lag/risk-return. Для каждого CORE-токена (не stable/staking, спарк ≥12): drawdown = насколько последняя цена ниже 40-дневного пика (0% = новый хай), и позиция в диапазоне low→high (0% = на дне, 100% = на пике). Таблица отсортирована по самому глубокому просаду; колонка drawdown окрашена corrColor (глубже = краснее), горизонтальный бар показывает позицию в диапазоне с маркером. Внизу авто-вывод: самый просевший + ближайший к своему хаю.

Применение: глубокий drawdown у нижней границы диапазона = выбит вниз (вотчлист на mean-reversion); у верхней = моментум/брейкаут. Дополняет risk-return (там vol/Sharpe), не дублирует.

Новая чистая функция `drawdowns()` + helper `bar()`. Вызывается раз на boot (`riskReturn(); drawdowns();`) — spark статичен, в 60-сек refresh не входит.

**Данные на момент сборки (18 CORE-токенов, L=40):** самый глубокий просад **GTA −60%** (позиция 9% — у самого дна), на новых 40-дн хаях **HMSTR и REDO** (0% drawdown, поз 100%). MAJOR показателен: −26% от хая, но позиция 1% — фактически на 40-дн минимуме.

**Блокер по пути (починен):** рабочее дерево было повреждено mount-truncation багом предыдущего прогона (#25-fail) — index.html/unit.test.mjs/ROADMAP.md обрезаны по хвосту (index 69988 vs HEAD 74796, unit 15093 vs 15964, ROADMAP оборвана на слове «требу»). Восстановлены из целого HEAD через `git show HEAD:f > f` (bash-redirect, проверено побайтово cmp). data/*.json (snapshots/scores/bots/index) — легитимные апдейты дневного CI, оставлены. **Для следующих прогонов:** записывать в репо только bash-redirect (`cat /tmp/f > f`) с read-back-проверкой cmp; Edit/Write усекают.

Тесты: 104/104 unit (вкл. +7 drawdown: dd=−50% на монотонном спуске, pos=0 у дна, dd=0/pos=100 на новом хае, defined, called-at-boot, CORE-фильтр). Real-data self-check прогнан на живых spark (18 токенов): все dd≤0, pos∈[0,100], конечны, вывод невырожден. API живые (TONAPI/STON.fi/DexScreener — 200). git из песочницы НЕ запускался — деплой через `deploy.bat`.

Следующее: лид-лаг с лагом 2 дня / разными горизонтами; momentum-leaderboard (RS-ранк по 7/14/30д окнам); добить hidden_buyer до p<0.05 (нужны дни); динамические веса по excess-WR в paper.py.

## ✅ Готово 20.06.2026 (evolve-run #26, Opus, scheduled) — требует deploy.bat

**index.html: Momentum leaderboard — RS-ранк (карточка `#momCard` после Drawdown radar).** Снят прямо из бэклога «momentum-leaderboard (RS-ранк по 7/14/30д окнам)».

**0 новых запросов** — переиспользует те же запечённые `spark[]` (~40 дневных закрытий), что и corr/lead-lag/risk-return/drawdown. Для каждого CORE-токена: % изменения цены за трейлинг 7 / 14 / 30 дней. Внутри каждого окна токены ранжируются по доходности → перцентиль (0–100); композит **RS = среднее перцентилей по доступным окнам** (100 = сильнейший в корзине на всех горизонтах, 0 = слабейший). Таблица отсортирована по RS, ячейки окон окрашены corrColor по доходности, авто-вывод: сильнейший + слабейший. Идея RS-ранка в том, что награждается лидерство на нескольких горизонтах, а не один удачный день — дополняет drawdown (там расстояние от хая), не дублирует.

Новые чистые функции: `winRet(s,n)` (% за n-дневное окно, null при нехватке истории/неположит. цене) и `momentum()`. Вызывается раз на boot (`drawdowns(); momentum();`) — spark статичен, в 60-сек refresh не входит.

**Данные на момент сборки (CORE-токены, L≈40):** изолированный real-data self-check на 4 реальных спарках (HMSTR/NOT/MAJOR/GTA): **HMSTR — сильнейший (RS 100: 7d +21%, 14d +92%, 30d +82%, ралли Hamster), GTA — слабейший (RS 0: 30d −47%)**, NOT нейтрально-плюсовой (RS 67), MAJOR слабый (RS 33). Все RS и доходности конечны, порядок верный, HMSTR на вершине — PASS. Короткое дневное окно → снимок тренда, не прогноз (в UI помечено).

**Блокеров по пути не было** — рабочее дерево пришло целым (git log чистый, файлы не усечены). Правки внесены Edit-инструментом (Windows-side), целостность хвоста подтверждена Read/Grep после каждой записи: `function momentum` (стр.722), `#momCard` (стр.243), boot-вызов (стр.492), `</html>` (стр.991) на месте. git из песочницы НЕ запускался — деплой через `deploy.bat`.

Тесты: изолированный real-data self-check (PASS, см. выше) + Read-tool верификация целостности. Тривиальная RS-математика, отдельный юнит не добавлял (conytail). API живые (TONAPI/STON.fi/DexScreener — 200).

Следующее: momentum — добавить RS-стрелку тренда (RS сейчас vs RS неделю назад, ускоряется/тормозит); лид-лаг с лагом 2 дня; risk-return — размер точки = mcap; динамические веса по excess-WR в paper.py.

## ✅ Готово 20.06.2026 (evolve-run #27, Opus, scheduled) — требует deploy.bat

**index.html: Market breadth — рыночные «внутренности» корзины (карточка `#brCard` после Momentum leaderboard).**

**0 новых запросов** — переиспользует те же запечённые `spark[]`, что и corr/lead-lag/risk-return/drawdown/momentum. Все предыдущие карточки — пер-токен (кто выиграл/проиграл); это первый и единственный рыночный калибр: **risk-on / risk-off прямо сейчас**. Для окон 1 / 7 / 30 дней по всем CORE-токенам считает долю растущих (advancers, % up) и медиану доходности корзины. Горизонтальный бар (зелёная заливка = % растущих, серая риска на 50%), колонки %up и медиана окрашены corrColor, счётчик up↑/down↓. Авто-вывод по 7d: ≥60% растущих → risk-on, ≤40% → risk-off, иначе mixed, + пояснение что узкая ширина (ралли на нескольких именах) слабее широкой. Дополняет существующие карточки (там по-токенно), не дублирует — это агрегат.

Новая чистая функция `breadth()` (+ локальный helper медианы и бара). Переиспользует уже существующий `winRet(s,n)` (из momentum) и `corrColor`. Вызывается раз в конце `loadDex().then` (`...; momentum(); breadth();`, стр.499) — spark статичен, в 60-сек refresh не входит.

**Данные на момент сборки (self-check на 24 запечённых spark, L=40):** во всех трёх окнах ~73% серий растут (16↑/6↓ из 22 валидных), медиана 1d +0.6% / 7d +3.2% / 30d +7.1% — корзина широко risk-on на снимке. (В реальной карточке CORE-фильтр убирает стейблы, числа сместятся, но математика та же.)

**Блокеров по пути не было** — рабочее дерево пришло целым (git log чистый). Правки внесены Edit-инструментом (Windows-side), целостность подтверждена Read/Grep после каждой записи: `function breadth` (стр.760), `#brCard` (стр.250), boot-вызов (стр.499), `</html>` (стр.1034) на месте.

Тесты: real-data self-check на 24 живых spark (PASS: %up∈[0,100], up+dn≤n, медиана конечна во всех окнах) + 6 юнит-ассертов на winRet/median (winRet +100%/−25%/too-short/zero-guard, median odd=2 even=2.5). API живые (TONAPI/STON.fi/DexScreener — 200). git из песочницы НЕ запускался — деплой через `deploy.bat`.

Следующее: breadth — добавить спарклайн равновзвешенного индекса корзины (TON Quant Index); momentum RS-стрелка тренда; лид-лаг с лагом 2 дня; risk-return — размер точки = mcap.

## ✅ Готово 20.06.2026 (evolve-run #26, Opus, scheduled) — требует deploy.bat

**index.html: Momentum oscillator — Wilder RSI(14) (карточка `#rsiCard` после Market breadth).**

**0 новых запросов** — переиспользует те же запечённые `spark[]` (~40 дневных закрытий), что corr/lead-lag/risk-return/drawdown/momentum/breadth. Для каждого CORE-токена (спарк ≥15): классический RSI Уайлдера на 14 дневных закрытиях — ограниченный осциллятор 0–100. <30 = перепродан (продажи выдохлись, вотчлист на mean-reversion); >70 = перекуплен (ралли растянуто). Таблица отсортирована от самого перепроданного; колонка RSI окрашена `corrColor` (низкий = зелёный/возможность, высокий = красный), плюс горизонтальный gauge с зелёной (0–30) / серой / красной (70–100) зонами и маркером, и колонка zone. Внизу авто-вывод: самый перепроданный + самый перекупленный.

**Зачем, если уже есть momentum leaderboard:** лидерборд ранжирует *сырой* % изменения (RS-перцентиль); RSI меряет *качество* движения — токен может лидировать по доходности и при этом быть перегрет (RSI>70). Ортогонально и leaderboard, и drawdown.

Новые чистые функции: `rsi(s,n)` (Уайлдер: SMA-инициализация за n, затем рекурсивное сглаживание `ag=(ag*(n-1)+gain)/n`; edge: al=0 → 100, обе 0 → 50, спарк<n+1 → null). Вызывается раз на boot (`...; breadth(); rsiGauge();`, стр.499) — spark статичен, в 60-сек refresh не входит.

**Блокеров по пути не было** — рабочее дерево пришло целым. Правки внесены Edit-инструментом (Windows-side), целостность подтверждена Read после каждой записи: `function rsi`/`rsiGauge` на месте, `#rsiCard` (стр.~257), boot-вызов (стр.499), `</html>` (стр.1077) на месте. **Подтверждено снова:** bash-маунт усекает не только запись, но и **чтение** хвоста — `wc -l index.html` через bash показал 878 строк, Read-tool видит реальные 1077. Поэтому проверка целостности — только Read-tool'ом.

Тесты: real-data self-check на 22 живых spark (PASS: RSI∈[0,100] у всех, расхождение с независимой эталонной реализацией Уайлдера ≤2.8e-14) + edge-кейсы (монотонный рост→100, спад→0, флэт→50, короткий→null — все верны). API живые (TONAPI/STON.fi/DexScreener — 200). git из песочницы НЕ запускался — деплой через `deploy.bat`.

Следующее: RSI — пометить дивергенцию (цена новый хай при падающем RSI); MACD-гистограмма на тех же spark; momentum RS-стрелка тренда; лид-лаг с лагом 2 дня; risk-return — размер точки = mcap.

## ✅ Готово 21.06.2026 (evolve-run #28, Opus, scheduled) — требует deploy.bat

**index.html: TON Quant Index — равновзвешенный индекс корзины (карточка `#tqCard` после Market breadth, в paneMom).**

**0 новых запросов** — переиспользует те же запечённые `spark[]`, что corr/lead-lag/risk-return/drawdown/momentum/breadth/RSI. Это первый и единственный **рыночный график-траектория** (остальные карточки — таблицы снимков). Равновзвешенный, ежедневно ребалансируемый индекс по всем CORE-токенам: каждый день каждый токен получает равный вес, поэтому отслеживается путь *типичного* токена, а не мега-капов (в отличие от treemap/mcap-взвешивания). Окно = самая длинная доступная серия, кэп 40 дней; база 100 = начало окна. SVG-спарклайн (полилиния + заливка 8% + пунктир базы 100 + маркер конца), цвет зелёный/красный по знаку полного движения. Подпись: текущее значение, 7d / 30d / полное окно в %.

**Зачем, если уже есть Market breadth:** breadth даёт *снимок* (% растущих сейчас); индекс даёт *траекторию во времени* — компаундит ежедневные равновзвешенные доходности в одну линию. Ортогонально: breadth = ширина движения, индекс = накопленный путь корзины.

Новая чистая функция `tqIndex()`: per-step равновзвешенная средняя дневная доходность (выравнивание серий по правому краю = «сегодня»; токены короче окна выпадают из ранних шагов естественно), кумулятивный продукт → ряд индекса, затем инлайн-SVG. Вызывается раз на boot (`...; rsiGauge(); tqIndex();`, стр.540) — spark статичен, в refresh не входит.

**Данные на момент сборки (real-data self-check, 18 CORE-токенов, W=40):** база 100 → **125.3** (полное окно +25.3%, 7d +6.95%, 30d +31.4%) — широкий risk-on, согласуется со снимком breadth. PASS: все 40 точек конечны и > 0; независимый пересчёт последнего шага совпал (diff 0); 1d-изменение = вычисленному шагу (diff ~5e-15); все 18 токенов имеют полное покрытие на старте окна.

**Блокеров по пути не было** — рабочее дерево пришло целым (git log чистый). Правки внесены Edit-инструментом (Windows-side), целостность подтверждена Read-tool'ом после каждой записи: `function tqIndex` (стр.873), `#tqCard` (стр.279), boot-вызов (стр.540), `</html>` (стр.1147) на месте. bash-маунт по-прежнему усекает чтение/запись хвоста — проверка только Read-tool'ом.

Тесты: real-data self-check на 18 живых spark (PASS, см. выше). Тривиальная кумулятивная математика, отдельный юнит не добавлял (conytail). API живые (TONAPI/STON.fi/DexScreener — 200). git из песочницы НЕ запускался — деплой через `deploy.bat`.

Следующее: TON Quant Index — переключатель окна (7/30/всё) и tooltip по точкам; mcap-взвешенный вариант рядом для сравнения «типичный vs капитал»; momentum RS-стрелка тренда; MACD-гистограмма; risk-return — размер точки = mcap.

## ✅ Готово 21.06.2026 (evolve-run #29, Opus, scheduled) — требует deploy.bat

**index.html: MACD trend — гистограмма MACD(12,26,9) (карточка `#macdCard` после TON Quant Index, в paneMom).** Снято прямо из бэклога «MACD-гистограмма» + закрывает идею «momentum RS-стрелка тренда» (rising/falling = ускорение/торможение моментума).

**0 новых запросов** — переиспользует те же запечённые `spark[]` (~40 дневных закрытий), что corr/lead-lag/risk-return/drawdown/momentum/breadth/RSI/index. Для каждого CORE-токена (спарк ≥35): классический MACD — EMA(12) − EMA(26) = линия MACD, 9-дневная EMA = сигнал, **гистограмма = MACD − сигнал**. Гистограмма > 0 = моментум набирает, < 0 = выдыхается; смена знака = свежий бычий/медвежий кроссовер; rising/falling (hist сейчас vs вчера) = ускоряется/тормозит. Таблица отсортирована по гистограмме (сильнейший тренд сверху), ячейка hist окрашена `corrColor`, колонка momentum (↑rising/↓falling), колонка state (positive/negative/bullish cross/bearish cross, кроссоверы выделены жирным+цветом). Авто-вывод: сильнейший + слабейший тренд + список свежих кроссоверов.

**Ключевое решение — нормировка к базе 100.** Сырая гистограмма MACD в абсолютных ценовых единицах НЕсравнима между токенами (токен по $0.0001 vs по $30). Поэтому каждый спарк сперва нормируется `x/spark[0]*100` → гистограмма в «пунктах 100-базного индекса», сравнима между токенами, сортировка осмысленна. Self-check доказал scale-invariance (умножение всех цен на 1000 → идентичная гистограмма).

**Зачем, если есть RSI и momentum leaderboard:** RSI — ограниченный осциллятор (перекуплен/перепродан), leaderboard — сырой RS-перцентиль доходности; MACD — трендследящий: расхождение быстрой/медленной EMA + кроссоверы как сигнал смены. Гистограмма даёт ускорение/замедление и тайминг разворота, чего нет у остальных. Ортогонально.

Новые чистые функции: `ema(a,n)` (стандартная EMA, k=2/(n+1)) и `macdHist(s)` (нормировка → линия → сигнал → гистограмма; <35 точек → null). Вызывается раз на boot (`...; tqIndex(); macd();`, стр.545) — spark статичен, в refresh не входит.

**Данные на момент сборки (изолированный real-data self-check на 4 живых спарках):** **HMSTR сильнейший** (hist +5.80, rising — ралли Hamster), GTA +0.70 rising (недавний отскок от дна 2.4e-5→2.85e-5 — гистограмма меряет недавний моментум, не полный возврат окна), NOT +0.85 fading, **MAJOR −0.19** (нисходящий). PASS: EMA совпала с независимой эталонной реализацией (diff <1e-9), scale-invariance подтверждена (×1000 → diff <1e-9), все значения конечны, короткий спарк → null, late-ramp → hist>0.

**Блокеров по пути не было** — рабочее дерево пришло целым (`tqIndex` из #28 закоммичен в HEAD; git status «M index.html/ROADMAP» оказался артефактом bash-маунта, усекающего хвост — Read-tool показал файлы целыми). Правки внесены Edit-инструментом (Windows-side), целостность подтверждена Read-tool'ом: `function macd`/`macdHist`/`ema` (стр.915–945, `function ema` ровно один — нет редекларации), `#macdCard` (стр.284), boot-вызов (стр.545), `</html>` (стр.1189) на месте. **Подтверждено снова:** bash git/чтение усекают хвост — вся проверка целостности только Read-tool'ом.

Тесты: изолированный real-data self-check (PASS, см. выше). API живые (TONAPI/STON.fi/DexScreener — 200). git из песочницы НЕ запускался — деплой через `deploy.bat`.

Следующее: MACD — пометить дивергенцию (цена новый хай при падающей гистограмме); risk-return — размер точки = mcap; TON Quant Index — переключатель окна; лид-лаг с лагом 2 дня; динамические веса по excess-WR в paper.py.

## ✅ Готово 21.06.2026 (evolve-run #30, Opus, scheduled) — требует deploy.bat

**index.html: Momentum leaderboard — колонка `trend` (RS-стрелка тренда).** Прямо из бэклога run #26 «momentum — добавить RS-стрелку тренда (RS сейчас vs RS неделю назад, ускоряется/тормозит)».

**0 новых запросов** — переиспользует те же запечённые `spark[]`. Раньше карточка показывала только мгновенный RS-ранг; теперь видно *направление*: токен набирает или теряет относительное лидерство. RS считается дважды на одной корзине: «сейчас» — на полном спарке, «неделю назад» — на спарке, усечённом на последние 7 закрытий (`spark.slice(0,len-7)`), тот же перцентильный ре-ранк. `trend = RS_now − RS_prev`: ▲ зелёная при >+3, ▼ красная при <−3, → серая (плоско). Авто-вывод дополнен: кто сильнее всего ускоряется / затухает.

Рефактор: логика композита RS вынесена в чистую `rsComposite(retArrs)` (массив пер-токенных оконных доходностей → выровненный массив RS), вызывается для «сейчас» и «неделю назад». `momentum()` переписан под неё. Сигнатура и boot-вызов (`...; momentum(); breadth(); ...`, стр.545) не менялись — никакой новой проводки.

**Данные на момент сборки (изолированный real-data self-check, Node-порт точной JS-математики на 20 живых CORE-спарках):** все RS∈[0,100], все Δ конечны и ∈[−100,100], порядок сортировки верный. **HMSTR сильнейший (RS 96, trend −2 — лидер, но импульс чуть остыл), GTA слабейший (RS 0). Сильнее всего ускоряется COIN (+47 — скачок середины серии вышел в топ ранга), затухает FRT (−35).** PASS.

**Блокеров по пути не было.** Правки внесены Edit-инструментом (Windows-side), целостность подтверждена ТОЛЬКО Read-tool'ом: `function rsComposite` (стр.785), `function momentum` (стр.793), `<th>trend</th>` (стр.813), нота (стр.822), `</html>` (стр.1212) на месте. **Подтверждено снова:** bash-маунт усекает чтение хвоста cloud-only файлов (`wc -l` показал 1019 вместо 1212, index.json оборвался на 1496 байте) — вся проверка целостности через Read-tool.

Тесты: изолированный real-data self-check (PASS, см. выше). Нетривиальная часть (двойной ре-ранк со сдвигом) покрыта одним прогоном на настоящих данных, как требует conytail; тривиальная стрелочная математика — без отдельного юнита. API живые (TONAPI/STON.fi/DexScreener — 200). git из песочницы НЕ запускался — деплой через `deploy.bat`.

Следующее: лид-лаг с лагом 2 дня / разными горизонтами; TON Quant Index — переключатель окна; MACD — пометить дивергенцию; динамические веса по excess-WR в paper.py.

## 💰 Готово 22.06.2026 (evolve-run #31, Opus, scheduled) — ДЕНЕЖНЫЙ ПИВОТ — требует deploy.bat

**Новый приоритет (от Игоря 22.06):** каждый цикл 80% — на заработок (paper-бот + поиск/валидация стратегий), 20% — поддержка. Решать самому, диверсифицировать. Read-only аналитика теперь второстепенна.

**Аудит paper-бота (10 дней, 12–21.06):** `cons` 1000→990 (≈флэт), `aggr` ~1000→**853 (−15%)**, 2/11 побед. Слив `aggr` — на сигналах, которые скоринг метит как **noise**: momentum −$54, accum_div −$43, flow_imbalance −$29. По excess-vs-market эдж есть **только у hidden_buyer**: x1 WR 83%, x3 +3.9% excess, **walk-forward ПОДТВЕРЖДЁН** (ins +3.3 / oos +3.0). Остальные — noise или collecting.

**Денежная правка (scripts/paper.py): распределение капитала по измеренному эджу.** Раньше `score_mults` (скаляр) влиял только на ранжирование входа, а walk-forward-подтверждённый hidden_buyer получал verdict «neutral» → штраф ×0.8 (недоинвест в единственную доказанную альфу). Теперь `load_score_mults()` возвращает `(rank_mult, size_mult)`:
- `RANK_MULTS` (приоритет входа): edge 1.5 / neutral 0.8 / noise 0.3 / collecting 1.0; **walk-forward-confirmed → ×1.4** (приоритет на дефицитные open-слоты, даже если точечный verdict лишь «neutral»).
- `SIZE_MULTS` (размер позиции, капится ≤1.0 чтобы буст не пробил impact-safe базу): edge/neutral 1.0 / **collecting 0.6** (непроверенное щупаем меньшим капиталом) / **noise 0.0** (ноль, поверх рангового блока — ремень и подтяжки).

Эффект: капитал концентрируется в OOS-подтверждённой альфе, непроверенные сигналы рискуют на 40% меньше, шум полностью отрезан.

**Self-check (изолированно, на живом scores.json + сухой end-to-end прогон в песочнице, БЕЗ записи в репо):** маппинг даёт hidden_buyer (1.12, 1.0) — приоритет+полный размер (было 0.8, штраф снят); momentum/accum_div (0.3, 0.0) — заблокированы; dip_reversal/flow_imbalance/liq_inflow (1.0, 0.6) — щуп. Полный прогон `paper.py` на копии данных: компилируется, отрабатывает до финального print, equity не изменилась (сегодня только momentum=noise → бот корректно НЕ открыл ничего, сохранив капитал). PASS.

**Баг по пути (починен):** строка print `noise=[... if m<1.0]` падала бы с TypeError после перехода value→tuple; исправлено на `if sz==0.0`. Пойман сухим прогоном.

**Целостность:** правки Edit-инструментом (Windows-side), подтверждено Read-toolّ'ом (`RANK_MULTS`/`SIZE_MULTS` стр.42–44, `load_score_mults` стр.46, финал стр.227) + успешный end-to-end прогон отработал весь файл до последнего print. bash-маунт по-прежнему усекает чтение хвоста cloud-only файлов — проверка только Read-tool/прогоном. git из песочницы НЕ запускался — деплой через `deploy.bat`.

Следующее (денежное): (1) добить hidden_buyer до p<0.05 — нужно больше дней, это растящаяся альфа; (2) добавить 1–2 новых сигнала под early-detection (листинги/accum раньше толпы) для будущей продажи алертов; (3) копи-трейдинг — трекать топ-кошельки через TONAPI; (4) excess-weighted continuous sizing (размер ∝ величине excess, а не ступенькой).

## 💰 Готово 22.06.2026 (evolve-run #32, Opus, scheduled) — требует deploy.bat

**Денежная фича (scripts/paper.py): excess-weighted continuous sizing.** Прямо из бэклога #31 «(4) excess-weighted continuous sizing (размер ∝ величине excess, а не ступенькой)». 80/20-цикл, денежный приоритет: правится сам торговый движок, а не read-only аналитика.

**Что было:** `SIZE_MULTS` — ступенчатая таблица по вердикту (edge/neutral 1.0 / collecting 0.6 / noise 0.0). Размер позиции не зависел от *величины* измеренного эджа: сигнал с excess +0.5% и сигнал с +3.9% при одном вердикте получали одинаковый капитал.

**Что стало:** размер позиции теперь **непрерывен по измеренному base-horizon excess** (`x` из scores.json). Логика `load_score_mults()`:
- **walk-forward-подтверждённая альфа → полный impact-safe размер (1.0)** — ремень из #31 сохранён (единственная доказанная альфа деплоится целиком).
- остальные положительные → `eff_x = x·n/(n+SIZE_SHRINK)` (усадка малой выборки к 0 — оценка по n=1 это в основном шум), затем `size = clip(eff_x/SIZE_X_FULL, FLOOR, 1.0)`. Капится ≤1.0 (impact-safe), пол `SIZE_FLOOR=0.25` (любой положительный эдж хотя бы щупаем).
- **noise ИЛИ непол��жительный excess (x≤0) ИЛИ n=0 → 0.** Раньше collecting с измеренным минусом (liq_inflow x−2.1, dip_reversal x−4.4) всё равно щупались на 0.6; теперь капитал течёт только в измеренно-положительный эдж.

Константы: `SIZE_X_FULL=4.0` (excess %, дающий полный размер), `SIZE_SHRINK=3.0`, `SIZE_FLOOR=0.25`. Удалена таблица `SIZE_MULTS`.

**Почему безопасно концентрировать жёстче:** скоринг (`score.py`) считает эдж из journal сигналов + форвардных цен, **независимо от того, открыл ли бот позицию**. Поэтому обнуление размера у непроверенных/минусовых сигналов НЕ останавливает обучение — journal продолжает писать и скорить каждый детект. Концентрация капитала ничем не вредит сбору данных.

**Self-check (изолированно, на живом scores.json + сухой end-to-end прогон в песочнице, БЕЗ записи в репо):** маппинг — hidden_buyer (1.12, **1.0** — валид. альфа, полный размер), flow_imbalance (1.0, **0.25** — маргинальный плюс n=1 → пол-щуп), momentum/accum_div/liq_inflow/dip_reversal **(_, 0.0)** — обнулены (noise или x≤0). Ассерты: все size∈[0,1], валид. альфа=1.0, noise+минус=0, маргинальный=floor — PASS. Полный прогон `paper.py` на копии данных: компилируется, отрабатывает до финального print, equity не изменилась (cons 990.22 / aggr 853.21 — сегодня только momentum=noise → бот корректно НЕ открыл ничего, капитал сохранён). PASS.

**Целостность:** правки Edit-инструментом (Windows-side), подтверждено Read-tool'ом — `SIZE_X_FULL/SHRINK/FLOOR` (стр.50–52), `load_score_mults` (стр.54–79, непрерывная логика на месте), финальный print с `zero_size:` (стр.245), хвост `main()` цел. **bash-маунт по-прежнему усекает чтение/запись хвоста cloud-only файлов** (compile bash-копии показал 227 строк vs реальные ~250 у Read-tool; `git diff`/`tail` в bash ложно «теряют» #31) — вся проверка целостности ТОЛЬКО Read-tool'ом + прогоном на чистой копии. git из песочницы НЕ запускался — деплой через `deploy.bat`.

Следующее (денежное): (1) добить hidden_buyer до p<0.05 (растёт с днями); (2) копи-трейдинг — топ-кошельки через TONAPI (главная следующая денежная фича); (3) калибровать `SIZE_X_FULL`/`SIZE_SHRINK` по факту, когда наберётся ≥20 закрытых сделок; (4) excess-weighted *exit* (раньше резать слабеющий эдж).

## 💰 Готово 23.06.2026 (evolve-run #33, Opus, scheduled) — требует deploy.bat

**Денежная фича (scripts/paper.py): excess-weighted EXIT (edge-fade).** Прямо из бэклога #32 «(4) excess-weighted exit — раньше резать слабеющий эдж». 80/20, денежный приоритет: правится торговый движок, симметрично входу из #31/#32.

**Что было:** позиция держалась до tp / sl / time(max_days) / rug_exit. Если измеренный эдж сигнала-основания обнулялся (вердикт→noise или excess≤0), бот всё равно досиживал позицию до таймаута — голый бета-риск без альфы, занятый дефицитный open-слот.

**Что стало:** один новый выход `edge_fade` в `run_bot` — позиция закрывается досрочно, если у её сигнала текущий `size_mult == 0.0` (тот же гейт, что уже блокирует ВХОД в #31/#32: noise-вердикт ИЛИ непол��жительный excess). Порядок проверок: `rug_exit → tp → edge_fade → sl → time`. tp проверяется первым (выигрыш всегда забираем), затем edge_fade режет мёртвый эдж до того, как позиция доедет до sl/таймаута. Принцип симметрии: «перестань держать то, что сейчас бы не купил».

**Почему это денежно и безопасно:** капитал высвобождается из позиций без измеренного эджа и течёт обратно в подтверждённую альфу (hidden_buyer). Скоринг (`score.py`) пишет journal по каждому детекту независимо от позиций — досрочный выход ничему не мешает учиться. Размер кода: одна `elif`-строка + комментарий, ноль новых констант/запросов (переиспользует `score_mults`, уже загружаемый для входа).

**Self-check (изолированно, реконструированная копия paper.py + копия живых данных в песочнице, БЕЗ записи в репо):** инъекция двух синтетических открытых позиций на живом снапшоте — momentum (вердикт noise, size_mult 0.0) и flow_imbalance (size_mult 0.25, контроль). Прогон `paper.py`: momentum закрыт с `reason=edge_fade`, flow_imbalance остался открыт. Ассерты PASS. Текущие `zero_size` на живом scores.json: accum_div, dip_reversal, liq_inflow, momentum → именно эти сигналы теперь триггерят edge_fade у открытых позиций; hidden_buyer (1.0) и flow_imbalance (0.25) — не трогаются. Скрипт компилируется и отрабатывает до финального print.

**Баг по пути:** не было. **Блокеров:** не было.

**Целостность:** правка Edit-инструментом (Windows-side), подтверждено Read-tool'ом — `edge_fade` (стр.139–142), хвост `main()` цел (стр.245–253, `noise=[...]`/print/`__main__`/`main()`). **bash-маунт по-прежнему усекает чтение хвоста cloud-only paper.py** (`wc -l` показал 244 vs реальные 253, `cp` дал битую копию с обрывом на `noise = [s for s`) — поэтому self-check гонялся на реконструированной из контекста копии, а целостность репо-файла проверена ТОЛЬКО Read-tool'ом. git из песочницы НЕ запускался — деплой через `deploy.bat`.

Следующее (денежное): (1) копи-трейдинг — топ-кошельки через TONAPI (главная следующая денежная фича, накопилось 3 цикла в очереди); (2) добить hidden_buyer до p<0.05 (растёт с днями); (3) калибровать `SIZE_X_FULL`/`SIZE_SHRINK` + порог edge_fade по факту ≥20 закрытых сделок; (4) логировать долю edge_fade-выходов в отчёте бота, чтобы видеть, не слишком ли часто режет (анти-churn-метрика).
